# Changelog

## Unreleased
- Initial specification import.
- Changed level 4 user configuration discovery from `~/.config/<app_name>/config.{json,toml}` to `~/.<app_name>/config.{json,toml}` so the user-level application directory can also contain other application-specific files.
